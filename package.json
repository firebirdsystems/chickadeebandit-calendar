{
  "name": "chickadee-app-calendar",
  "version": "1.0.0",
  "description": "Calendar app for Family Hub",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
