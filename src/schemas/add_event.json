{
  "type": "array",
  "items": [
    { "type": "string", "minLength": 1, "maxLength": 200, "description": "Event title" },
    { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$", "description": "Start date (YYYY-MM-DD)" },
    { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$", "description": "End date (YYYY-MM-DD)" },
    { "type": ["string", "null"], "pattern": "^\\d{2}:\\d{2}$", "description": "Start time (HH:MM) — omit or null for all-day event" },
    { "type": ["string", "null"], "maxLength": 1000, "description": "Description (optional)" }
  ],
  "minItems": 3,
  "maxItems": 5
}
